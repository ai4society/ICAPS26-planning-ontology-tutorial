repos:
  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: 0.11.21
    hooks:
      - id: uv-lock

      - id: uv-export
        args:
          - --frozen
          - --format=requirements.txt
          - --output-file=requirements.txt
          - --no-dev
