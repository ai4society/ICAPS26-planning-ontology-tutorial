# Data

Sample assets used by the notebooks.

- `domains/`: PDDL domains, problems, and example plans (such as `blocksworld/`).
- `kgs/`: prebuilt RDF knowledge graphs (`.ttl`) populated from IPC data, used by
  the planner-selection and explanation notebooks.

Keep these files small so notebooks load fast in Colab.
